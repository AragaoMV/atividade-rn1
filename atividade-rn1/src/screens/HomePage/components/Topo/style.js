import { StyleSheet } from "react-native";

export const styles = () => {
    return StyleSheet.create({
        container: {
            padding: 10,
        },
        titulo: {
            fontSize: 20,
            fontWeight: "bold",
        },
        subtitulo: {
            fontSize: 12
        }
    })
} 